# BigShort Radar 环境变量模板
# 用法:cp .env.example .env, 然后逐项填入。.env 不会进 git。
#
# 必填 / 可选 见下方 [REQUIRED] / [OPTIONAL] 标记。
# 不填会怎样的影响也写在每条注释里。

# ─────────────────────────────────────────────────────────────
# [REQUIRED] FRED API Key — St. Louis Fed 经济数据
# ─────────────────────────────────────────────────────────────
# 用途:拉取 19 条指标里的 14 条(VIX/收益率曲线/HY-IG OAS/SOFR/USDJPY/DXY/JP10Y/
#       WALCL/ON RRP/TGA/FRA-OIS/中国外储/USDCNY/CNY10Y)
# 申请:https://fred.stlouisfed.org/docs/api/api_key.html (免费,1 分钟,只需注册邮箱)
# 不填:主流程 daily_fetch 全废,只剩 yfinance 路径(VIX/VVIX/SKEW 等),且 yfinance 现已严重限速
FRED_API_KEY=

# ─────────────────────────────────────────────────────────────
# [REQUIRED] 时区(影响 launchd 定时任务对齐美东收盘)
# ─────────────────────────────────────────────────────────────
TZ=Asia/Shanghai

# ─────────────────────────────────────────────────────────────
# [REQUIRED] Flask 端口
# ─────────────────────────────────────────────────────────────
FLASK_PORT=5050
FLASK_DEBUG=0

# ─────────────────────────────────────────────────────────────
# [OPTIONAL] LLM API — 用于每日风险简报 + dashboard 浮窗 chatbot
# ─────────────────────────────────────────────────────────────
# 不填:dashboard 顶部"今日风险简报"和右下角 chatbot 浮窗会显示"未配置",其他全部功能不受影响
#
# 推荐方案:阿里百炼 Coding Plan (OpenAI 协议兼容,中文对话好)
#   申请:https://bailian.console.aliyun.com/  (国内,需实名,有免费额度)
#   开通"Coding Plan"后:
#     DASHSCOPE_BASE_URL=https://coding.dashscope.aliyuncs.com/v1
#     DASHSCOPE_MODEL=qwen3-coder-plus  (实测稳定;qwen-max 不在 Coding Plan 白名单)
#
# 备选:OpenAI / DeepSeek / 任何 OpenAI 协议兼容服务
#   只需把 BASE_URL 和 MODEL 改成对应值,API key 设到 DASHSCOPE_API_KEY 即可
DASHSCOPE_API_KEY=
DASHSCOPE_BASE_URL=https://coding.dashscope.aliyuncs.com/v1
DASHSCOPE_MODEL=qwen3-coder-plus

# ─────────────────────────────────────────────────────────────
# [OPTIONAL] GitHub token — 仅 ralph_loop.sh 自动 push 用
# ─────────────────────────────────────────────────────────────
# 用途:让 ralph loop 跑完后自动 commit + push 到 GitHub
# 申请:GitHub Settings → Developer Settings → Personal access tokens (classic)
#       勾 "repo" scope,有效期按需选
# 不填:ralph loop 跑完不会自动 push,需手动 push;主流程不受影响
#
# 注意:不要把 token 写在这里!写在 .ralph/.token 文件里(也是 gitignored)。
#       只是这里提示一下需要这个 key。
