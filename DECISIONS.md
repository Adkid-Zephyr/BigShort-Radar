# DECISIONS

> ADR 风格。每条一行：日期 / 决定 / 原因。重大架构与选型变更必须在这里留痕。

## 2026-05-15

- 选 SQLite 而非 Postgres：MVP 单机够用，零运维。后续上云若数据量超 1GB 再考虑迁移
- 选 Flask 而非 FastAPI：MVP 不需要异步与自动文档，模板渲染更直接
- 不引入 ORM：表结构稳定且简单，原生 SQL 维护成本更低
- 不上 Docker：本地开发 macOS 直跑速度最快
- 端口 5050（避开常用 5000）
- Python 3.9+ 兼容：用户机器上 Python 3.9 是系统默认
- 时间统一 UTC 入库，展示时转东八区
- 阈值默认值放代码常量，正式校准后写 INDICATORS.md，二者出现差异以 INDICATORS.md 为准
- 凡需注册账号才能拿 API key 的数据源（目前主要是 FRED），相关任务整体延后；用户后续会一次性把 key 写进 `.env`，届时再开做。PLAN.md 受影响项后缀 `⏸ 待 API key`，工作循环遇到时跳过往下找
- thresholds.classify 边界规则：`up` 方向 value==low→GREEN / value==high→YELLOW（要严格 > high 才 RED）；`down` 方向 value==high→GREEN / value==low→YELLOW（要严格 < low 才 RED）。与 INDICATORS.md 的 yield_curve_10y2y "RED < 0 / YELLOW 0–0.5 / GREEN > 0.5" 一致
- P0 首条上线指标走 **VIX via yfinance**（不需 API key，能立即让里程碑跑通）。FRED 路径并行推进，等用户给 key 后再做 10Y-2Y
- **yield_curve_10y3m 阈值采用 A 案**（与 10Y-2Y 同口径）：GREEN >0.5 / YELLOW 0–0.5 / RED <0，方向 down。理由：保持收益率曲线类指标横向可比；10Y-3M 历史波幅虽更大，但同切分有助于 dashboard 一眼看出"曲线维度"整体颜色一致性。后续若样本数据显示阈值需校准，再走 ADR 调整
- **iter 21 store helper 抽象触发**："重复三次再抽象"原则——vix.py / yield_curve.py / yield_curve_10y3m.py 三处共用的"遍历 series + NaN/Inf 跳过 + upsert"循环抽到 `src/store/db.py::upsert_series_from_pandas(conn, name, source, series) -> int`。三处 fetch_and_store 改为单行调用。db.py 保持零外部库依赖（NaN 用 `v != v` 判，Inf 用直接比较）
- **hy_oas 阈值采用推荐方案**：GREEN <4 / YELLOW 4–8 / RED >8，方向 up。理由：HY OAS（高收益债期权调整利差，单位百分点）历史分位——平静期 3–4%（2017、2021）、紧张期 5–7%（2022 末）、危机期 ≥10%（2008=18 / 2020 春=11）。8% 是市场已定价显著违约风险的临界点，4% 是利差正常上沿。后续视实测样本走 ADR 校准
- **ig_oas 阈值采用推荐方案**：GREEN <1.5 / YELLOW 1.5–3 / RED >3，方向 up。理由：IG OAS（投资级期权调整利差，单位百分点）历史分位远窄于 HY——平静期 0.8–1.5%（2021）、紧张期 2–3%（2022 末）、危机期 ≥5%（2008=6.5 / 2020 春=4.0）。3% 是 IG 市场已显著紧张的临界点（远高于经济周期上沿），1.5% 是平静上沿。HY/IG 两条对照能区分"系统性信用收缩"与"高收益独自承压"
- **vix_term_structure 阈值采用推荐方案**：GREEN <0.95 / YELLOW 0.95–1.0 / RED >1.0，方向 up。计算 VIX/VIX3M 比值。理由：contango（远月波动率溢价）是市场常态（比值 ~0.85–0.92），比值跨过 0.95 进入紧张区，跨过 1.0 转 backwardation 即近月恐慌定价超过 3 个月——历史上 2008 雷曼周、2020 春、2022 多次都曾跨过。指标本身没有"绝对量级"，0.95/1.0 切点稳定可参考
- **sofr_iorb 阈值采用推荐方案**：GREEN <0.05 / YELLOW 0.05–0.15 / RED >0.15（绝对值，bp），方向 up。计算 |SOFR - IORB|（或 SOFR - IORB 的正向偏离）。理由：SOFR 与 IORB 利差本应紧贴 0（联储靠 IORB 上限+ON RRP 下限框住 SOFR），偏离 5bp 已是流动性异常信号、偏离 15bp+ 即货币市场失灵（2019/9 回购危机当日 SOFR 飙到 IORB+300bp）。本指标用 abs(spread) 让"上下穿透"都能触发；后续若需区分上偏（融资紧张）/下偏（准备金过剩）再拆两个指标
- **iter 27 LLM 接入决策**：服务商=阿里百炼 Coding Plan（用户授权），协议=OpenAI 兼容 `/v1/chat/completions`，Auth=Bearer key 写 .env DASHSCOPE_API_KEY；模型 `qwen-max` 在 Coding Plan endpoint 实测不支持（400 invalid_parameter_error），改用 `qwen3-coder-plus`（实测可用，单次 ~300 字简报 prompt+output 19 tokens 量级）。依赖只用 requests（白名单内），不引 openai/anthropic SDK。失败优雅降级：LLM 不可用 daily_fetch 主流程不受影响

- **iter 28 跨市场维度首批指标决策**：先上 USDJPY（FRED:DEXJPUS 145/160）、DXY 广义（FRED:DTWEXBGS 110/125）、日本 10Y（FRED:IRLTLT01JPM156N 月值 1.0/2.0）三条，方向均为 up。USDJPY 不走 yfinance（有 rate limit）走 FRED 官方日值。日本 10Y 用 OECD 月值 IRLTLT01JPM156N（FRED 没免费日值 JGB）。BoJ 资产规模（JPNASSETS）暂搁——绝对量级阈值意义低，需要做"同比变化率"派生指标，等综合温度计做完后回头补
- **iter 29 综合温度计权重决策**：曲线 25% / 信用 25% / 跨市场 20% / 流动性 15% / 波动率 15%，合计 100%。理由：曲线是衰退最强先行（10Y-2Y 倒挂后 ~14 个月衰退）；信用是危机定价最快反应；跨市场抓 2025-26 主剧本（日元 carry / 强美元）；流动性是危机引爆器但平时低噪；波动率作弱权重（VIX 偶尔失灵）。Level → 分数线性（GREEN=0 / YELLOW=50 / RED=100），同 group 内算术平均，再按权重加权。总分阈值：< 25 GREEN / 25-65 YELLOW / ≥ 65 RED

## 2026-05-17

- **iter 33 [THESIS] 投资论点文档化**：用户反馈"按颜色和分数分组不够客观"，且发现项目 PROMPT/HANDOFF 都缺投资论点的根。基于用户与 Claude Opus 2026-05-14 对话沉淀正式 `THESIS.md`，包含：(1) 论点演化（"赌一波"→"穿越周期+尾部赚一笔"，Universa 路线 vs Burry 路线）；(2) 5 个候选崩盘剧本（美元荒/国债基差/日本 carry/AI 泡沫/信用估值滞后崩）；(3) 5 个反共识结构性观察；(4) §6 列出 9 类按"对崩盘监控价值"排序的缺失内容（历史回测 / z-score / 加速度 / 组合信号 / 融资市场维度 / 政策反应维度 / 波动率结构 / 跨市场补全 / 前端升级 / 仓位建议 / 失效条件）。同步更新 PROMPT.md（第一原则段 + 文档同步纪律 6 条检查清单）/ HANDOFF.md（必读清单加入 THESIS）/ README.md（顶部引用）/ PLAN.md（重排 P3 加 P3.6 真实优先级）。**核心纪律变化**：从此每轮代码改完后必须主动过 6 条文档同步清单，不等用户提醒

- **iter 34a ralph loop 脚手架**：用户要求参考 snarktank/ralph 引入自动迭代循环 + 多模态自检防幻觉。决定：不重写本项目已有的 PROMPT/HANDOFF/PLAN 循环架构（已比 ralph 完整），只补 ralph 三件 codebuddy code 还没做的事——(1) 新建 `scripts/ralph_loop.sh`：bash 主循环，参数 `max_iter`（默认 10），每轮启 `codebuddy -p --max-turns 80 -y`，喂入 `.ralph/loop_prompt.md`；(2) `.ralph/loop_prompt.md`：单轮 prompt 模板（必读 8 文件 / 暂停清单 / 文档同步 6 条 / 输出协议 LOOP_OK/LOOP_BLOCKED/LOOP_FAIL）；(3) `.ralph/progress.log`：追加式单行历史（与 last-summary.md 覆盖最新轮互补，对应 ralph 的 progress.txt）。兜底三道防线：BLOCKED.md 停 / pytest 红写 BLOCKED 停 / iteration.txt 没 +1 停。**与 ralph 原版差异**：用 PLAN.md 顶项替代 prd.json；记忆三层（last-summary 覆盖 + progress.log 追加 + DECISIONS ADR）；BLOCKED.md 替代 fail-skip。multimodal 自检（playwright 截图 + Read 看图）放 iter 34b 单独一轮做。同步 PROMPT.md / HANDOFF.md / PLAN.md

- **iter 34b multimodal 自检**：ralph loop 防幻觉的最后一道线。`scripts/visual_check.sh` 起 Flask:5050 + 用 `playwright-cli`（chromium）截 dashboard 全图 1440×900 + 抓 DOM 快照 + console 日志 + Flask log，全部输出到 `.ralph/visual_check_iter<N>/`。`.ralph/visual_check_template.md` 6 个 section（改动摘要/自检命令/看图判断必查项/看图发现问题/结论 PASS-FAIL-PARTIAL/TODO），由 agent 用 Read 读 dashboard.png 后逐项填到 `.ralph/visual_check_iter<N>.md`。`.ralph/loop_prompt.md` 加 §4.5：改动 `templates/` 或 `src/web/` 必须跑这一步。设计要点：(1) 浏览器没装时 graceful 退出 rc=1 并给指引（chromium 需 `playwright-cli install-browser chromium`，国内首次安装可能因下载源被墙需要代理/mirror，本轮安装确实卡了 10+ 分钟未完成，留给用户处理）；(2) `--no-flask` 参数支持复用已起的 dashboard；(3) 自动 cleanup（trap EXIT 停 Flask + 关浏览器 session）；(4) `tests/test_visual_check.py` 7 用例验脚本骨架不真启浏览器。视觉判断本身不能用 pytest assert——这是 multimodal agent 的 Read 工具职责，是 pytest 的补充而非替代。`.gitignore` 加 `.ralph/visual_check_iter*/` 与 `.playwright-cli/`

- **iter 35 指标 source 可点击外链**：用户反馈"既然数据源都弄好了，每个项目应该可以点击跳转到数据源官网去看"。设计：(1) 新建 `src/web/source_links.py`：纯函数 `source_url(source: str) -> str | None`，按前缀分发——`FRED:` → `fred.stlouisfed.org/series/<id>`、`YF:`/`YAHOO:` → `finance.yahoo.com/quote/<urlencoded-ticker>`（^VIX 必须 quote 成 %5E）、`OECD:` → 搜索页、`CBOE:` → `cboe.com/tradable_products/<lower>/`；未知前缀 / 多源派生（含逗号）/ 空 ident 全返 None。(2) `_INDICATOR_REGISTRY` 加可选 `source_url` 字段：派生指标（VIX 期限结构 = VIX/VIX3M）和 source 字符串骨头不对的（`FRED:SOFR-IORB` 这条 series id 不存在）手填权威 URL；其余靠 source_links 自动推导。(3) 模板 `<a class="source-link" target="_blank" rel="noopener noreferrer">`，蓝色 #60a5fa + hover 变深 + ::after ↗ 箭头。Tests：`test_source_links.py` 14 用例（含 caret 编码 / 大小写无关 / 多源拒绝 / 边界 None）+ `test_web.py` 1 个 e2e 断言。已知边角：empty 行 colspan=4 吞掉 source 列，下一轮单独修

- **iter 36 公开化推送 GitHub + empty 行 source 链接**：合并两件事到一轮。(A) empty 行 source：模板 `colspan=4` → `colspan=3` + 独立 source 列展示"查源"链接（registry 手填 source_url 时显示，否则 `—`）。test_web 加 1 用例验 cboe.com 在 empty 路径里出现。(B) GitHub 推送：用户决策——public 仓库 `Adkid-Zephyr/BigShort-Radar`、CC BY-NC 4.0、THESIS 双份处理。具体执行：(1) 新建 `THESIS_PUBLIC.md` 脱敏版（删个人桌面路径、§5.3 仓位映射保留方法论但不写绝对金额、加 §9 免责声明），（私有 `THESIS.md` 留本地不进 git）；(2) `.gitignore` 加 `THESIS.md`，git rm --cached 解 track；(3) `git filter-branch --index-filter` 重写整段 git 历史扫净 THESIS.md（推送前备份完整目录到 `~/finance-radar.backup-YYYYMMDD_HHMMSS`，确认 git ls-files 无 THESIS.md 后才 push）；(4) `LICENSE` CC BY-NC 4.0 含项目特定免责段；(5) README.md 全量重写——targets 接力开发者 + 公开访客双视角，含 当前进度快照 / 架构 ASCII / 10 指标阈值表 / 综合温度计算法 / LLM 集成 / launchd / Ralph Loop / Multimodal 自检 / 一分钟启动 / API key 申请 / 文件导览 / 路线图，全文用 THESIS_PUBLIC 替代 THESIS 引用；(6) 路径相对化：PROMPT/HANDOFF 里 `/Users/lau/finance-radar/` → `<repo-root>`；(7) GitHub repo 创建走 API（curl POST /user/repos）+ push 用 `git -c http.extraHeader="Authorization: Basic $(base64 user:token)"`，token 不进任何 remote URL / config 文件 / 日志。**关键**：用户的 token 在对话里出现过 = 已暴露，但用户拍板 push 后不吊销。push 后 remote URL 仍为干净 https，无 token 残留

- **iter 37 README 文风重写为技术文档版 + 第二阶段路线图（iter 38–50）**：用户反馈 iter 36 那版 README "AI 味太浓"，要"超详细的技术文档版，但简洁不废话"。Web 调研对比 6 个金融/量化开源项目（AKShare/Zipline/Qlib/QSTrader/yfinance/FinanceDatabase）后总结文风规则：(1) 一句话简介改事实陈述，不用"X is a Y that does Z"模板，参考 Zipline 短句节奏；(2) 完全去 emoji（除徽章），Qlib 那种 15+ 个 emoji 是反例；(3) 章节扁平化（最多 H2，避免 H3+ 嵌套）；(4) 工程师口吻 70% + 价值阐述 30%；(5) 删营销词（"赋能/极致/强大/优雅/为人类"等）。重写后 246 行（含表格代码块），17 H2，0 emoji，0 营销词。**第二阶段路线图（iter 38–50，14 轮）**：用户拍板"扩到 26 条 + 异常监测多视角 + 历史可视化"。核心决策：(1) 20 条新指标全上 + akshare 中国维度 6 条 = 总 26 条 / 7 维度（原 5 + 政策 + 中国），但其中国债基差杠杆 / USD basis swap / TIC / 日 30Y 暂搁（HTML 爬虫或无免费源）；(2) 异常监测多视角分 10 个候选（Sparkline / Z-score / 加速度 / 风险矩阵热力图 / 综合温度计 2 年时间线 / 异常事件流 / 组合信号告警 / 同环比对比 / 政策对冲对比 / 阈值校准面板），路线图按"行内增强→新页面→规则层"分层展开；(3) 引入独立 `data/historical_cache.sqlite`（不动主 DB）+ `fetch_history` 通用接口，一次性回填 5 年数据，所有 sparkline / Z-score / 回测都基于此；(4) **akshare 加白名单走单独 ADR**（iter 38 第一件事），它是新依赖触发暂停清单；(5) **THESIS §6.1 历史回测**后置到 iter 51+，原最高优先级让位于"先扩指标再校准"用户决策。本轮不动代码，pytest 197/197 不变

- **iter 38 akshare 引入决策（方案 B：不引）**：评估 akshare 加白名单触发 PROMPT.md 暂停清单第 1 条，写 BLOCKED.md 给用户拍板。评估数据：akshare 1.18.60，requires_python ≥3.9（兼容），pandas 2.3.3 ≥2.0（兼容），但**直接依赖 28 个**（项目当前 8 个，扩张 ×4.5），关键传染依赖含 mini-racer（嵌入式 V8 JS 引擎，数十 MB）、lxml、html5lib、curl_cffi、xlrd、openpyxl 等爬虫栈，安装时间 ~30 秒 → 5-10 分钟。三个候选方案：A 全引（拿 6 条中国指标，依赖暴涨）/ B 仅 FRED 能拿到的 3 条（依赖零增长）/ C 迟做。**用户拍板方案 B**：仅加 FRED 系列的中国维度 3 条（中国外汇储备 FRED:TRESEGCNM052N 月值 / USDCNY 在岸 FRED:DEXCHUS / 中国 10Y FRED:IRLTLT01CNM156N 月值），不引 akshare。代价：放弃上证 PE / USDCNH 离岸 / 北向资金（这 3 条信号"对崩盘监控价值"中等，不属 THESIS §3.2 五剧本核心）；USDCNH 与在岸 USDCNY 信号 ~90% 重合。结论：**总指标终态从 26 条下调到 19 条，7 维度不变**。akshare 引入路径保留——若未来 1-3 个月运行后用户判断"中国信号粒度不够"，再单独走 ADR 重启评估

- **iter 39 Sparkline 90 天微折线**：首发异常监测视角（PLAN.md P3.6 视角 A）。设计要点：(1) `src/web/sparkline.py` 纯函数 `build_sparkline_svg(values, threshold_low, threshold_high, direction, width=120, height=28)` 出 inline SVG，零依赖（不引 plotly/matplotlib）；(2) 三档阈值带：up 方向 RED 在顶 / down 方向 GREEN 在顶（按 y_of(threshold) 排序，鲁棒不假设 low<high 数值大小）；(3) 末点蓝色高亮 `<circle r=2>`；(4) 数据不足 MIN_POINTS=10 个 → "积累中 (n/10)"灰色占位 SVG；(5) NaN/Inf 过滤保留有效值；(6) `_INDICATOR_REGISTRY` 加 threshold_low/high/direction 字段引用 indicator 模块常量（避免重复定义）；(7) `_fetch_sparkline_values` 先 history cache DB（5 年历史），不够 10 个点走主 DB get_series 兜底，仍不够出占位；(8) Flask `create_app` 加 `history_db_path` 参数支持测试注入；(9) 模板加"90 天"列，empty 行 colspan 调整。23 个新测试（占位 / 阈值 / 方向 / 鲁棒性 / e2e）。**实测**：8 条指标渲染真实折线（5 年回填数据），2 条占位（VIX Yahoo 限速漏拉 / SOFR-IORB 派生跳过 cache）。HTML 从 20KB → 32KB，全是 SVG 数据

- **iter 40 Sparkline 可点击跳详情页 + 多页架构起点（base.html + nav）**：用户反馈"折线图应该可以点击跳转大图放大缩小"+"前端可以多几个 page 或者模块"。决策：(1) 大图技术选 **Plotly 后端生成 div + CDN**（已在 requirements.txt 白名单，自带 zoom/pan/hover/导出 PNG）；(2) iter 40 范围限 "Sparkline 可点击 + 详情页 + base.html + nav 起点"，同环比表后置 iter 41；(3) 主 dashboard sparkline 仍纯 SVG，**不引 Plotly**，主页加载速度不变。实施：拆 `templates/_base.html`（共享 head/CSS/nav/chatbot/{% block content %}）+ 改造 `templates/index.html` 继承 _base + 详情页 `templates/indicator_detail.html`（plotly 大图 + dl 元信息 + 面包屑）+ `src/web/charts.py` 纯函数 plotly to_html 含三档区域填色 + 阈值水平线 + 末点高亮 + `src/web/app.py` 加 `/indicator/<name>` 路由 + `_REGISTRY_BY_NAME` O(1) 索引 + `_fetch_history_pairs` 返 (dates, values)。21 个新测试。导航条 5 项："指标"（current）+ 事件/热力图/时间线/对冲面（disabled 占位）。pytest 277 → 288。**实测**：HY OAS 详情页 786 个 plotly y 数据点 5 年大图

- **iter 41 同环比对比表（行内异常监测视角 H）**：用户路线图原 iter 40 的同环比，因 iter 40 改做 sparkline 跳详情页 + 多页架构，本视角后置一轮做。设计：(1) `src/web/comparisons.py` 纯函数 `build_comparisons(dates, values, today_value, today_date, direction, lookbacks=(7,30,90))` → `{7: {...}, 30: ..., 90: ...}`，每个 dict 含 lookback_value / pct_change / abs_change / deteriorate；(2) `_nearest_value_on_or_before` helper：目标日期不存在时取 ≤ target 最近一条非 NaN 值；(3) "恶化"逻辑：direction=up + 上升 = 恶化（True），direction=down + 下降 = 恶化；(4) `_build_rows` 调 history pairs 时改成 days=120（覆盖 90 天回看），sparkline 仍只取最近 90，comparisons 用全 120；(5) `_build_rows` try/except 包 build_comparisons 防失败影响主流程；(6) 模板红字 #f87171 = 恶化，绿字 #4ade80 = 改善，灰色 — = N/A；(7) empty 行 colspan 4→6 重排版。22 个新测试（_parse_iso/_nearest/lookback/build_comparisons 边界全覆盖）。pytest 288 → 310。**实测**：HY OAS 当前 2.76，7 天前 2.79 = -1.1% 绿（改善）；yield_curve_10y2y 当前 0.50，90 天前 0.48 = +4.2%（曲线变陡，down 方向 = 改善 → good 绿）；总分布 8 bad / 18 good / 1 N/A
- **iter 42 Z-score 列（异常监测视角 B）**：`src/web/zscore.py` 纯函数 `compute_zscore(history_values, current_value, direction)` 返 {z, percentile, extreme, n}：(1) 需 ≥30 个有效样本；std=0 防除零返 None；(2) percentile 用中位法 (below + equal/2) / n × 100；(3) extreme 判定结合 direction：up 方向 z>2 = 危险；down 方向 z<-2 = 危险；其他 direction abs(z)>2；(4) NaN/Inf/None 全部过滤。`_build_rows` 单独拉 5 年（days=1825）long_values 算 z（不复用 sparkline 的 120 天），模板加 Z 列（|z|>2 + 方向匹配 → 红字 bad），hover title 显百分位 + n。17 个新测试（边界 / NaN 过滤 / direction 极端判 / 常数分布 / 百分位）。pytest 310 → 327。**实测分布**：日本 10Y +2.6σ 99 分位红字（历史最高位 = 危险）；其他指标 ±1σ 正常；样本数 n 反映 cache DB 回填长度（1249 = 5 年日值；786 = HY/IG OAS 3 年；59-60 = 月值指标 5 年）

- **iter 43 加速度列 Δ（异常监测视角 C）**：`src/web/acceleration.py` 最小二乘斜率：(1) `linear_slope(values)` 用公式 `slope = (n*Σxy - Σx*Σy) / (n*Σx² - (Σx)²)` 算单位 step 斜率，过滤 NaN/Inf；(2) `compute_acceleration(values, direction, short=5, long=20)` 比较短窗与长窗末端斜率（取 values 末尾 short+1 / long+1 个点）；(3) 加速恶化判定：短斜率方向与 direction 同向（up: 上升 / down: 下降）+ |短| > |长|；(4) 长斜率 0 时 ratio=None 防除零。`_build_rows` 加 acceleration 字段（用 spark_values 120 天即够），模板加 Δ 列：↗=加速恶化红字 / · =不加速 / —=数据不足，hover 显短长斜率/d 数值。14 个新测试（常数 / 线性升 / 线性降 / 加速 up / 加速 down / 反向不算危险 / NaN 过滤 / 长斜率 0）。pytest 327 → 341。**实测**：DXY 短 0.66/d > 长 0.31/d 标 ↗（美元加速走强 = up 方向恶化）；其他指标 · 表示加速度模式不显著

- **iter 44 政策反应维度 3 条 + 综合分权重重平衡**：(1) 三条新指标 `walcl/on_rrp/tga` 模块（同 dxy 模板）：WALCL 单位 millions，up 方向，阈值 8M-9M（QE 后稳态-危机救市量级）；ON RRP 单位 millions，**down 方向**（缓冲耗尽 = 危险），阈值 100K-500K（百万）；TGA 单位 millions，up 方向（吸流动性 = 危险），阈值 600K-1M。FRED series id 分别为 WALCL（周）/RRPONTSYD（日）/WTREGEN（周）。(2) 加进 `_INDICATOR_REGISTRY` + `_GROUP_ORDER`（"政策"插在流动性后跨市场前）+ daily_fetch FETCHERS + backfill_history TARGETS + briefing registry。(3) **综合分 _GROUP_WEIGHTS 重平衡**：原 5 维度权重 25/25/20/15/15 → 6 维度 22/22/18/13/13/12（曲线信用各让 3 给政策；流动性波动率各让 2；跨市场让 2）。理由：政策反应是 THESIS §4.1 反共识盲区核心，必须有权重；但 3 条新指标周值偏粗 + 历史数据短，相对其他维度赋予较低权重 12%。test_risk_score 同步更新阈值断言（15→13 / 25→22）。21 个新测试。pytest 341 → 362。**实测**：ON RRP 当前全在 RED 区底部（缓冲耗尽 = 真实流动性临界信号 = 第一次系统级别提示）；WALCL 6.8T 绿；TGA 1T 边界

- **iter 45 波动率结构 2 条 VVIX + SKEW**：`vvix.py`（YF:^VVIX up，阈值 90/120 — 历史正常 80-95，雷曼周 ~210）+ `skew.py`（YF:^SKEW up，阈值 130/145 — 历史均值 ~119，2017 中峰 154）。同 dxy 模板。注册到 web/daily_fetch/backfill。13 个新测试。pytest 362 → 375。**已知 Yahoo 限速**：5 年 backfill 失败（与 VIX 同问题），dashboard 现显"积累中"占位；解决方案：(a) 等 Yahoo 限速过期后手动重跑；(b) 每天 daily_fetch 累积单点（不批量）；(c) 后续 iter 改用 CBOE 直接 CSV 源（需新依赖评估）

- **iter 46 FRA-OIS 代理 + 中国维度 3 条**：(1) `fra_ois.py` 派生指标 `FRED:DGS3MO - FRED:SOFR`（3M T-Bill 减 SOFR）作为 LIBOR 退役后 FRA-OIS 代理。THESIS §4.3 揭示融资市场维度从 1 条扩到 2 条（含 SOFR-IORB）。阈值 0.10 / 0.30 百分点（10bp / 30bp，基于 2020/3 一度走阔到 ~50bp 历史峰值）。(2) "中国" 维度 3 条全部走 FRED 系列（iter 38 方案 B 决策）：china_fx_reserves(FRED:TRESEGCNM052N 月值，down 方向，阈值 3.0T/3.1T 美元，2015-16 资本外流期跌至 3.0T 触发市场紧张) + usdcny(FRED:DEXCHUS 日值，up 方向，阈值 7.10/7.30，2022/10 一度 7.32 历史新低) + china_10y(FRED:IRLTLT01CNM156N 月值，down 方向，阈值 2.0/2.5，2024-25 触及 1.7-1.8 通缩信号)。(3) `_GROUP_WEIGHTS` 第 3 次再平衡（22/22/13/13/18/12 → 20/20/14/12/14/10/10），加 "中国" 维度 10 权重 = 总 100；test_risk_score 阈值断言同步（13→12 / 22→20）。(4) `_GROUP_ORDER` 加"中国"在跨市场后估值前。25 个新测试。pytest 375 → 400。**实测**：USDCNY backfill 1246 日值，china_fx_reserves 57 月值；daily_fetch 跑 19 个 fetcher 0 失败；综合分从 GREEN 20.83 → YELLOW 26.33（含中国维度后切换 GREEN/YELLOW 边界）

- **iter 48 异常事件流（视角 F）**：`src/web/events.py` 三类事件检测：(1) 翻档（连续 2 天 level 不同，severity_map GREEN=0/YELLOW=1/RED=2 算 delta，恶化 alert，改善 info）；(2) 首次穿越阈值（cross_low / cross_high，仅记录窗口内首次发生）；(3) 单日突变（pct_change >5% + 朝 direction 同向 = warn 危险，反向 = info）；4) `merge_events` 多指标合并按 (date, name) 倒序。新页 `/events` 路由扫所有 _INDICATOR_REGISTRY 拉 60 天 history pairs（确保 30 天窗口前面有比对日）。模板继承 _base，CSS event-alert/warn/info 分色 + kind 不同 emoji（↗/↘/⚠/△/⚡）。nav "事件"项激活，主 dashboard 含 /events 链接。18 个新测试。pytest 400 → 418
